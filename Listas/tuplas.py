# é uma estrutura imutavel. a sua diferença para com a lista é justamente que não pode ser alterada.

# são criadas atraves da classe tuple ou colocando valores separados por virgula

carros = ("gol") 
print(isinstance(carros, tuple))