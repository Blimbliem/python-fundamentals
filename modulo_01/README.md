# Módulo 01: Fundamentos e Estruturas de Controle (Python)

Este módulo foca na transição do pensamento lógico humano para a execução algorítmica. O objetivo principal foi dominar o fluxo de dados e as tomadas de decisão de um software.

### 🧠 Conceitos Aprendidos:

* **Entrada e Saída de Dados:** Manipulação de informações via terminal utilizando `input()` e `print()`.
* **Tipagem de Dados:** Entendimento de como converter strings para tipos numéricos (`int` e `float`) para cálculos matemáticos.
* **Estruturas Condicionais (`if/else`):** Implementação de regras de negócio onde o software decide o caminho a seguir com base em comparações lógicas.
* **Modularização com Funções (`def`):** Criação de blocos de código reutilizáveis, organizando o programa e facilitando a manutenção.
* **Laços de Repetição (`while`):** Automação de tarefas repetitivas e controle de fluxo baseado em condições de parada e contadores.
* **Acumuladores e Estados:** Uso de variáveis para somar e monitorar dados ao longo de múltiplas repetições.
* **Tratamento de Erros:** Introdução ao `try/except` para tornar o software mais robusto contra entradas inválidas do usuário.

### 🛠️ Ferramenta Utilizada:
* **Linguagem:** Python 3
* **Ambiente:** VS Code